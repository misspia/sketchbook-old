const Sketch = (canvas) => {
  console.log('metaball sketch')
  console.log(canvas)
  const context = canvas.getContext('2d')
  console.log(context)
  // const canvas = document.createElement('canvas');
  // const context = canvas.getContext('2d');
  //
  // canvas.width =
  // entryElement.appendChild(canvas);
}

export default Sketch;
